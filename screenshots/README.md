Drop real screenshots here, then update the image paths in the root `README.md`.

Suggested shots (PNG, ~1600px wide, trim to the app window):

- `viewer.png` — a PDF open with the thumbnail sidebar and a couple of annotations
  (highlight + sticky note) visible.
- `forms.png` — an AcroForm-bearing PDF with a text field and dropdown filled in, Forms
  panel open on the right.
- `ocr.png` — the OCR dialog mid-run on a scanned page, or a scanned page with the
  invisible text layer selected (blue selection highlight) to show it's searchable.
- `excel-export.png` — the Excel export review grid with a detected table.

Keep this folder lightweight (a handful of PNGs, not a full gallery) — GitHub renders
README images fine, no need for more than 3-4.
